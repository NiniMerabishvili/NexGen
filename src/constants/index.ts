/**
 * Application constants
 */

// Marquee animation settings
export const MARQUEE_REPETITIONS = 4;

// Form validation
export const MIN_QUESTION_LENGTH = 10;

// Animation durations
export const ANIMATION_DURATION = {
  FAST: 200,
  NORMAL: 300,
  SLOW: 500,
} as const;

